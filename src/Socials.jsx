import React from "react";
import { Link } from "react-router-dom";

const Socials = ({ mailto, label }) => {
    return (
        <>
            
        </>
    );
};

export default Socials;